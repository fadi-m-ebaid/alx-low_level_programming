#include "main.h"

/**
 * mul - fuction to multiply 2 integers
 * @a: First input integer
 * @b: Second input integer to be multiplied with the first
 * Return: the result of multipliction of 2 integers
 */
int mul(int a, int b)
{
	return (a * b);
}
